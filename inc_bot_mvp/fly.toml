app = "pomogaibot"  # Уникальное имя вашего бота

[build]
  dockerfile = "Dockerfile"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256

# Переменные окружения
[env]
  MODE = "polling"

# Примечание: для polling не нужны порты